# 5x5 운동 추적 앱 구현 계획

## 📋 Phase별 구현 순서

### ✅ Phase 1: 프로젝트 구조 및 DB 설정 (우선순위 1)

**목표**: SQLite DB 스키마 생성 + 초기 데이터 시드

**구현 항목**:

1. `src/db/` 폴더 생성
   - `schema.sql` - 테이블 정의
   - `setupDatabase.ts` - DB 초기화
   - `seedData.ts` - 초기 데이터 삽입
2. `exercises`, `routines`, `routine_exercises`, `workout_sessions`, `workout_entries`, `workout_summaries` 테이블 생성
3. 기본 운동 종목 + A/B/C 루틴 데이터 삽입

**검증**: DB 파일 생성 확인, 초기 데이터 조회 테스트

---

### ✅ Phase 2: 오늘의 루틴 화면 (우선순위 2)

**목표**: 날짜별 루틴 계산 + 운동 리스트 표시

**구현 항목**:

1. `src/utils/` - 날짜/루틴 유틸리티
   - `getRoutineByDate()` - 날짜→루틴 매핑
   - `formatDate()` - 날짜 포맷팅
2. `src/screens/workout/TodayRoutineScreen.tsx`
   - 오늘 루틴 표시 (A/B/C/REST)
   - 풀업 + 해당 루틴 운동 리스트
   - 각 운동별 최근 기록 + 도전 무게 표시
3. `src/hooks/useTodayRoutine.ts` - 오늘 루틴 데이터 조회 훅

**검증**: 월/화/수... 각 요일별 올바른 루틴 표시

---

### ✅ Phase 3: 세트 입력 기능 (우선순위 3)

**목표**: 5세트 입력 UI + DB 저장

**구현 항목**:

1. `src/components/workout/SetInputTable.tsx` - 세트 입력 컴포넌트
   - 5세트 \* (weight + reps) 입력 폼
   - 완료 버튼
2. `src/hooks/useChallengeWeight.ts` - 도전 무게 계산 훅
   - 최근 세션 조회
   - 5x5 성공 여부 판단
   - 다음 도전 무게 계산 (+5kg or 유지)
3. `TodayRoutineScreen`에 세트 입력 모달 추가
4. 운동 완료 후 `workout_sessions` + `workout_entries` 저장

**검증**: 저장 후 DB 데이터 확인, 다음 도전 무게 정확도

---

### ✅ Phase 4: 주간/월간 리포트 (우선순위 4)

**목표**: 캘린더 + 비교 차트

**구현 항목**:

1. `WeeklyReportScreen.tsx` - 주간 리포트
   - A/B/C 루틴별 저번주 vs 이번주 비교
   - 주간 진행률 표시
2. `MonthlyCalendarScreen.tsx` - 월간 캘린더
   - 날짜별 세션 존재 표시
   - 날짜 클릭 → 상세 기록
3. 차트 라이브러리 설치: `react-native-chart-kit` or `recharts`

**검증**: 날짜별 데이터 정확히 표시

---

### ✅ Phase 5: 종목별 히스토리 (우선순위 5)

**목표**: 운동별 그래프 + 필터링

**구현 항목**:

1. `ExerciseListScreen.tsx` - 운동 목록
   - 운동별 최근 기록 표시
2. `ExerciseDetailScreen.tsx` - 상세 화면
   - 그래프 탭 (무게 추세)
   - 리스트 탭 (세션별 기록)
   - 성공률, 평균무게, PR 표시
3. `useExerciseRecords.ts` - 운동별 기록 조회 훅

**검증**: 필터별 데이터 정확도, 그래프 표시

---

### ✅ Phase 6: 설정 및 알림 (우선순위 6)

**목표**: 사용자 설정 + 푸시 알림

**구현 항목**:

1. `SettingsScreen.tsx`
   - 기본 증량값 변경 (5kg)
   - 운동별 시작 무게 설정
2. `expo-notifications` 설치 및 설정
   - 매일 오전 11시 알림
   - 주/월 리포트 알림

**검증**: 알림 수신 확인

---

## 📂 최종 폴더 구조

```
src/
├── db/
│   ├── schema.sql
│   ├── setupDatabase.ts
│   └── seedData.ts
├── screens/
│   └── workout/
│       ├── TodayRoutineScreen.tsx
│       ├── WeeklyReportScreen.tsx
│       ├── MonthlyCalendarScreen.tsx
│       ├── ExerciseListScreen.tsx
│       ├── ExerciseDetailScreen.tsx
│       └── SettingsScreen.tsx
├── components/
│   └── workout/
│       ├── ExerciseRow.tsx
│       ├── SetInputTable.tsx
│       ├── RoutineHeader.tsx
│       └── ChartView.tsx
├── hooks/
│   ├── useTodayRoutine.ts
│   ├── useChallengeWeight.ts
│   ├── useExerciseRecords.ts
│   └── useWorkoutSummary.ts
└── utils/
    ├── date.ts
    ├── routine.ts
    ├── challenge.ts
    └── format.ts
```

---

## 🚀 다음 스텝

**지금 바로 시작할 것**: Phase 1 (DB 설정)

1. DB 스키마 작성
2. setupDatabase.ts 구현
3. 초기 데이터 시드

준비되면 "Phase 1 시작"이라고 알려주세요!
